
document.querySelectorAll('.media-gallery').forEach(gallery=>{const main=gallery.querySelector('.main-media img'); gallery.querySelectorAll('.thumb').forEach(btn=>{btn.addEventListener('click',()=>{gallery.querySelectorAll('.thumb').forEach(b=>b.classList.remove('active')); btn.classList.add('active'); main.src=btn.dataset.src; main.alt=btn.dataset.alt||main.alt;});});});
const quiz=document.querySelector('[data-quiz]'); if(quiz){const data=JSON.parse(document.getElementById('quiz-data').textContent); const result=quiz.querySelector('.quiz-result'); quiz.querySelectorAll('button[data-answer]').forEach(btn=>btn.addEventListener('click',()=>{quiz.querySelectorAll('button').forEach(b=>b.classList.remove('active')); btn.classList.add('active'); const ids=data.results[btn.dataset.answer]||[]; const cards=ids.map(id=>document.querySelector(`[data-card-id="${id}"]`)?.outerHTML||'').join(''); result.innerHTML='<h3 class="serif">Your best match</h3><div class="product-grid">'+cards+'</div>'; result.scrollIntoView({block:'nearest',behavior:'smooth'});}));}
document.querySelectorAll('[data-demo-cart]').forEach(btn=>btn.addEventListener('click',e=>{e.preventDefault(); alert('Demo only: checkout is disabled until Maria approves Shopify implementation.');}));
